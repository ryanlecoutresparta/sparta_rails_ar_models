<%= form_with(model: pokemon, local: true) do |form| %>
  <% if pokemon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pokemon.errors.count, "error") %> prohibited this pokemon from being saved:</h2>

      <ul>
      <% pokemon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :pokemon_type %>
    <%= form.text_field :pokemon_type %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level %>
  </div>

  <div class="field">
    <%= form.label :trainer_id %>
    <%= form.number_field :trainer_id %>
  </div>

  <div class="field"> Are you human? Tick if you are!
    <%= form.check_box :human %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
